api_version: opendata-etl.definitions/v1
profile: lite
notes: |
  Template local manifest for a deployment repo. Replace the URL/ref with your
  definition repository and keep refs pinned for repeatable runs.
source_credentials:
  fake_source_reader:
    kind: aws_profile
    description: Example placeholder for an S3 fixture credential.
definitions:
  - name: example_collection
    url: "https://github.com/example-org/example-definition-repo.git"
    ref: "v0.1.0"
    schema: ex_housing
    protected: false
    depends_on: []
    enabled_datasets:
      - sample_csv
      - bundle_demo
